---
phase: 03-pr-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tests/test_lifecycle.py]
autonomous: true
requirements: [LIFECYCLE-01]
must_haves:
  truths:
    - Test scaffold exists for all lifecycle operations
    - Mock fixtures support PR creation, merge, approve, decline, request changes testing
    - Tests can run in isolation without Bitbucket API
  artifacts:
    - path: tests/test_lifecycle.py
      provides: PR lifecycle test suite
      min_lines: 150
  key_links:
    - from: test functions
      to: mock fixtures
      via: pytest fixtures
      pattern: def test_*(bitbucket_client)
---

<objective>
Create comprehensive test scaffold for PR lifecycle operations before implementing tools.

Purpose: Enable TDD approach for lifecycle tools; ensure all 5 operations have test coverage; establish mock patterns for POST endpoints.
Output: tests/test_lifecycle.py with 8+ test cases covering LIFECYCLE-01 through LIFECYCLE-05.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-pr-lifecycle/03-CONTEXT.md
@.planning/phases/03-pr-lifecycle/03-RESEARCH.md

## Existing Test Infrastructure

From tests/conftest.py (read to understand existing fixtures):
- `bitbucket_client` fixture provides authenticated client
- `mock_config` fixture provides test configuration
- Mock patterns established for GET endpoints

## API Patterns to Mock

From 03-RESEARCH.md:

**Create PR (POST /pullrequests):**
```python
{
    "title": "string",
    "source": {"branch": {"name": "feature-branch"}},
    "destination": {"branch": {"name": "main"}},
    "description": "string",
    "close_source_branch": true
}
# Returns: Full PR object with id, links.html.href
```

**Merge PR (POST /pullrequests/{id}/merge):**
```python
{
    "type": "string",
    "message": "string",
    "close_source_branch": true,
    "merge_strategy": "merge_commit"  # or fast_forward, squash
}
```

**Approve PR (POST /pullrequests/{id}/approve):**
```python
# No body, returns Participant object
```

**Decline PR (POST /pullrequests/{id}/decline):**
```python
# No body, returns PR object
```

**Request Changes (POST /pullrequests/{id}/request-changes):**
```python
# No body, returns Participant object
```

## Required Tests

Per 03-RESEARCH.md test map:
1. test_create_pr_success - LIFECYCLE-01
2. test_create_pr_validation_error - LIFECYCLE-01
3. test_merge_pr_success - LIFECYCLE-02
4. test_merge_pr_state_validation - LIFECYCLE-02
5. test_approve_pr_success - LIFECYCLE-03
6. test_approve_pr_duplicate - LIFECYCLE-03
7. test_decline_pr_success - LIFECYCLE-04
8. test_decline_pr_state_validation - LIFECYCLE-04
9. test_request_changes_success - LIFECYCLE-05
10. test_request_changes_with_comment - LIFECYCLE-05
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test_lifecycle.py with PR lifecycle mocks and fixtures</name>
  <files>tests/test_lifecycle.py</files>
  <behavior>
    - Mock PR creation returns PR object with id, title, source/dest branches, links.html.href
    - Mock merge validates PR state (OPEN ok, MERGED/DECLINED fail with 409)
    - Mock approve validates state and prevents duplicates
    - Mock decline validates state and prevents duplicates  
    - Mock request-changes validates state
    - All mocks use requests-mock pattern consistent with existing tests
  </behavior>
  <action>
    Create tests/test_lifecycle.py with:

    1. Import pytest, requests_mock, and test fixtures from conftest

    2. Add mock response fixtures for each operation:
       - `mock_pr_response()` - Returns sample PR dict with id=123, title, branches, links
       - `mock_participant_response()` - Returns participant dict for approve/request-changes
       - `mock_merged_pr_response()` - Returns PR with state=MERGED
       - `mock_declined_pr_response()` - Returns PR with state=DECLINED

    3. Implement test cases:
       - test_create_pr_success: POST /pullrequests returns 201 with PR object, verify response parsing
       - test_create_pr_missing_title: Verify 400 error handling
       - test_merge_pr_success: POST /pullrequests/123/merge with merge_strategy, verify 200
       - test_merge_pr_already_merged: GET returns MERGED, merge POST should fail with state validation
       - test_merge_pr_declined: GET returns DECLINED, merge POST should fail with state validation
       - test_approve_pr_success: POST /pullrequests/123/approve returns 200
       - test_approve_pr_already_merged: State validation prevents approval
       - test_decline_pr_success: POST /pullrequests/123/decline returns 200
       - test_decline_pr_already_merged: State validation prevents decline
       - test_request_changes_success: POST /pullrequests/123/request-changes returns 200
       - test_request_changes_with_comment: Both request-changes POST and comment POST succeed

    4. Follow existing test patterns from test_read_operations.py:
       - Use requests_mock fixture for HTTP mocking
       - Assert on response status codes
       - Mock both GET (for state validation) and POST (for action) endpoints
       - Include error cases with proper HTTP status codes (400, 401, 404, 409)

    Follow patterns: requests-mock for HTTP, pytest fixtures, descriptive test names, assert on key fields.
  </action>
  <verify>
    <automated>pytest tests/test_lifecycle.py -x -v</automated>
  </verify>
  <done>All 10+ test cases pass; tests cover success, validation, and error paths for all 5 lifecycle operations</done>
</task>

</tasks>

<verification>
- [ ] tests/test_lifecycle.py exists
- [ ] File imports pytest and requests_mock
- [ ] Mock fixtures for PR responses defined
- [ ] Tests for create_pr success and validation
- [ ] Tests for merge_pr success and state validation (MERGED/DECLINED)
- [ ] Tests for approve_pr success and duplicate prevention
- [ ] Tests for decline_pr success and state validation
- [ ] Tests for request_changes success and with optional comment
- [ ] pytest tests/test_lifecycle.py -x passes
</verification>

<success_criteria>
- Test scaffold exists for all 5 lifecycle operations
- All tests can run in isolation with mocked responses
- Test patterns follow existing codebase conventions
- pytest tests/test_lifecycle.py -x passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-pr-lifecycle/03-01-SUMMARY.md`
</output>
